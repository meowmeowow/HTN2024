export default function profile(){


//set goalfsdklfjsdlkf
} 
const Motivators = ({navigation, route}) => {
    return <Text>This is {route.params.name}'s profile</Text>;
  };
  
  const Favorite_Jams = ({navigation, route}) => {
    return <Text>This is {route.params.name}'s profile</Text>;
  };
  
  const Dreadful_Jams = ({navigation, route}) => {
    return <Text>This is {route.params.name}'s profile</Text>;
  };
  
      